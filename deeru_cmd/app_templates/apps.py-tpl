from django.apps import AppConfig


class {{ app_camel_name }}Config(AppConfig):
    name = '{{app_name}}'

    # 下面几项暂时没用

    # 类型
    deeru_type='{{deeru_type}}'

    # 别名，插件、主题列表中显示的名字
    nice_name=''

    url=''

    author=''

    description=''




